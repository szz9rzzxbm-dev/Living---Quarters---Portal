/**
 * Supabase client init — NOT connected yet.
 *
 * This is a frontend-only build. When the backend is wired up, install
 * `@supabase/supabase-js` and initialise the client from environment variables:
 *
 *   import { createClient } from '@supabase/supabase-js'
 *   export const supabase = createClient(
 *     import.meta.env.VITE_SUPABASE_URL,
 *     import.meta.env.VITE_SUPABASE_ANON_KEY,
 *   )
 *
 * Until then, components read from src/lib/mockData.js.
 */

export const supabase = null
