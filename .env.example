# Copy to .env.local and fill in once the backend is connected.
# Frontend-only build does not require any of these yet.

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Stripe (publishable key — test mode first)
VITE_STRIPE_PUBLISHABLE_KEY=
